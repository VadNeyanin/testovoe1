---
# argument spec file for v_neyanin.httpscript.docker-install

argument_specs:
  main:
    short_description: Role description.
    options:
      docker-install_my_variable:
        type: str
        description: A simple string argument for demonstration.
        default: "default_value"
